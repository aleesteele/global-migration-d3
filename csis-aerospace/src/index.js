import * as d3 from 'd3'

import parseData from './js/data'
import internationalAstronautDatabase from './data/international-astronaut-database.csv'
import './scss/main.scss';

const astronauts = parseData.data({

})

function drawChart() {
  if (breakpoints.isMobile()) {

  }

  map.init({
    
  })
}
